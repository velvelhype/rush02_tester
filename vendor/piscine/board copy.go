package piscine

/*
const MAX = 64

type Board struct {
	field       []uint64
	length      int
	fieldLength int
}

func (board *Board) shift(x int) {
	var tmp uint64
	for i := range board.field {
		board.field[i] >>= x
		tmp = board.field[i] >> (MAX - x)
	}
}
*/
